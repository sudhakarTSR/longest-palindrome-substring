**API for storing and retrieving the longest palindrome substring in a string**

Endpoints:

POST /palindrome/{input}
    - Calculates the longest palindrome substring and stores it in DB

GET /palindrome
    - Returns the stored palindrome substring